<header class="header">
  <h3 class="text-center title-text custom-font">Ember Weather</h3>
    {{#if session.isAuthenticated}}
      <a {{ action 'invalidateSession' }}>Logout</a>
    {{else}}
      {{#link-to 'login'}}Login{{/link-to}}
    {{/if}}
</header>

  {{outlet}}

<footer class="footer">
  <p class="text-center title-text">this is the footer</p>
</footer>